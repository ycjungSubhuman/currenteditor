using System;
using System.IO;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEditor;
using UnityEngine;

namespace Assets.Timeline.Utility
{
    public class ClipAdder
    {
        [MenuItem("Assets/Create/New Clip for CurrentWriter")]
        public static void Add()
        {
            /*var wavPath = EditorUtility.OpenFilePanel("Select WAV file", "", "wav");
            Debug.Log(wavPath);
            var midPath = EditorUtility.OpenFilePanel("Select MID file", "", "mid");
            Debug.Log(midPath);*/
        }
    }
}
